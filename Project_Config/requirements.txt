# requirements.txt
# Plant Diagnosis v2 — dependencies
# Pillow is pinned to avoid the recurring 12.x torchvision incompatibility.

torch>=2.1.0
torchvision>=0.16.0
timm>=1.0.7
numpy>=1.24
Pillow==10.4.0
matplotlib>=3.7
tqdm>=4.66
